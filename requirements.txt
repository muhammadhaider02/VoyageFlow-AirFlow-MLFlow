# TitanFlow-AirFlow-MLFlow — Python Dependencies
# Install with: pip install -r requirements.txt
# (Only needed if running outside Docker)

apache-airflow==2.8.1
mlflow==2.11.1
scikit-learn==1.4.1.post1
pandas==2.2.1
numpy==1.26.4
psycopg2-binary==2.9.9
